# Vome 插件规范（纯前端 · vome-plugin-front）

> 目录：`plugins/vome-plugin-front` · key：`scaffold-frontend`  
> **用法 / Agent**：见 [`.vscode/skills/SKILL.md`](./.vscode/skills/SKILL.md)

## 可用 / 不可用

| 能力 | 状态 |
|------|------|
| Vue3 + Vite 微应用、`menus` / wujie | **可用** |
| `hostRequest`（Bearer + 无感 refresh） | **可用** |
| 可选 EPS：`bootHostEps` + `service.*` | **可用** |
| Pinia、Hash 自动路由、宿主主题 / 语种同步 | **可用** |
| 整包打入 `vome-core/admin` CRUD UI | **不推荐** |
| 声明 `hook` / `routes` / `server` | **禁止** |
| `vite base: '/'` | **禁止**（须 `base: './'`） |
| 插件内切语言 UI | **禁止**（跟随 Admin） |

## 目录与清单

| 路径 / 字段 | 约定 |
|-------------|------|
| 源码 | `src/` → 构建产物 `web/` |
| 语言包 | 包根 `locales/zh-CN.json`（`pack` 必须打进 `.vome`） |
| `module.json.key` | 与 `PLUGIN_KEY`、`menus[].appKey` **一致** |
| `menus[].appKey` | **必须**等于 `key`；安装后挂到侧栏 **「无界渲染」** 下 |
| `menus[].icon` | **勿填**（微应用页无 icon；父菜单由宿主写入 `ri-artboard-fill`） |

## Vue（强制）

| 项 | 约定 |
|----|------|
| 路由 | **仅** `createWebHashHistory` |
| 页面 | `src/pages/**/*.vue` 自动路由；`_` 前缀跳过 |
| 入口壳 | `App.vue` 须挂主题 / 语种同步 |
| 别名 | `@` → `src` |

## 主题 / 语种（强制）

| 项 | 约定 |
|----|------|
| 根节点 | `html/body/#app` **透明**；禁止再铺全页白底 |
| 样式 | 用宿主 CSS 变量（`--brand` / `--foreground` / `--border`…） |
| 主题事件 | bus `vome-host-theme`；优先 parent 实时值，勿死用过期 props |
| 语种事件 | bus `vome-host-locale` + `props.locale` |
| 文案 | `t('a.b')`；译包权威为宿主 `i18n_pack`（`scopeType=plugin`） |

## 产物

`pack` → `web/` + `locales/` + `module.json`（及 assets/README）。勿含 `node_modules` / `src`。

## 交付自检

- [ ] `bun run pack`，产物可安装；侧栏可进
- [ ] 亮/暗与语种跟宿主；无双层白卡片
- [ ] 无插件内切语言 UI；`appKey` = `key`；`base: './'`
